#version 460
out vec4 FragColor;

in vec2 uvs;

uniform sampler2D sampler;

void main()
{             
    float depthValue = texture(sampler, uvs).r;
    
	FragColor = vec4(vec3(depthValue), 1.0);
   
}